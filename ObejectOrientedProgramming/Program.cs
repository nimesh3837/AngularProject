using System;

namespace ObejectOrientedProgramming
{
    class Program
    {
        static void Main(string[] args)
        {
            Inheritance.Test();
            // Abstration.Test();
            //Tuple.Test();
            //Generics.Test();
            // SealedClass testSealed = new SealedClass(); // sealed class can be instantiated but not inherited or extended
            // testSealed.SealedMethodCheck();
            //StaticCLass.StaticMethodCall();// static method can be called without instantiation
        }
    }
}
